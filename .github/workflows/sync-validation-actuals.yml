name: Sync frozen-model actuals

on:
  schedule:
    # September validation window: 3:00 PM and 9:00 PM Pacific (PDT).
    - cron: "00 22 * * *"
    - cron: "00 04 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-frozen-model-actuals
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Refresh recent actual counts only
        run: python3 scripts/sync_validation_actuals.py --days 14 --embed-index
      - name: Publish refreshed validation actuals
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/validation/latest_actuals.json data/validation/post_freeze_fish_counts.json index.html
          git diff --cached --quiet || git commit -m "Refresh frozen model validation actuals"
          git push
